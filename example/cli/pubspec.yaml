name: litertlm_dart_cli
description: CLI smoke example for litertlm_dart (pure Dart, no Flutter).
publish_to: none
version: 0.0.1

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  litertlm_dart:
    path: ../..

dev_dependencies:
  lints: ^6.0.0
